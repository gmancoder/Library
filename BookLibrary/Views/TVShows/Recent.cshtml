@model BookLibrary.Models.ViewModels.RecentTVShowViewModel
@using BookLibrary.Models
@{
    ViewBag.Title = "Recently Added TV Shows";
    ViewBag.SubTitle = ViewBag.TotalItems + " Total TV Shows";
    ViewBag.PageImage = "/Content/Images/tv_shows.png";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-12 pull-right" style="text-align:right;">
        @foreach (string page in ViewBag.Pages)
        {
            @Html.Raw(page);
        }
    </div>
</div>
@if(Model.Today.Count() > 0)
{
    <h3><strong>Today</strong></h3>
    int idx = 0;
    <div class="row">
        @foreach (var tvShow in Model.Today)
        {

            <div class="col-md-2" style="text-align:center;">
                <a href="/TVShows/Details/@tvShow.Id">
                    <img src="~/Content/Images/tvShows/@tvShow.DisplayImage" width="150" title="@tvShow.Title" alt="@tvShow.Title" /><br />
                    @tvShow.Title
                </a>
            </div>
            idx += 1;
            if (idx % 6 == 0)
            {
                @:</div><div class="row">
            }
        }
    </div>
}
@if (Model.Yesterday.Count() > 0)
{
    <h3><strong>Yesterday</strong></h3>
    int idx = 0;
    <div class="row">
        @foreach (var tvShow in Model.Yesterday)
        {

            <div class="col-md-2" style="text-align:center;">
                <a href="/TVShows/Details/@tvShow.Id">
                    <img src="~/Content/Images/tvShows/@tvShow.DisplayImage" width="150" title="@tvShow.Title" alt="@tvShow.Title" /><br />
                    @tvShow.Title
                </a>
            </div>
            idx += 1;
            if (idx % 6 == 0)
            {
                @:</div><div class="row">
            }
        }
    </div>
}
@if (Model.ThisWeek.Count() > 0)
{
    <h3><strong>This Week</strong></h3>
    int idx = 0;
    <div class="row">
        @foreach (var tvShow in Model.ThisWeek)
        {

            <div class="col-md-2" style="text-align:center;">
                <a href="/TVShows/Details/@tvShow.Id">
                    <img src="~/Content/Images/tvShows/@tvShow.DisplayImage" width="150" title="@tvShow.Title" alt="@tvShow.Title" /><br />
                    @tvShow.Title
                </a>
            </div>
            idx += 1;
            if (idx % 6 == 0)
            {
                @:</div><div class="row">
            }
        }
    </div>
}
@if (Model.LastWeek.Count() > 0)
{
    <h3><strong>Last Week</strong></h3>
    int idx = 0;
    <div class="row">
        @foreach (var tvShow in Model.LastWeek)
        {

            <div class="col-md-2" style="text-align:center;">
                <a href="/TVShows/Details/@tvShow.Id">
                    <img src="~/Content/Images/tvShows/@tvShow.DisplayImage" width="150" title="@tvShow.Title" alt="@tvShow.Title" /><br />
                    @tvShow.Title
                </a>
            </div>
            idx += 1;
            if (idx % 6 == 0)
            {
                @:</div><div class="row">
            }
        }
    </div>
}
@if(Model.Older.Count() > 0)
{
    foreach(KeyValuePair<int, List<TVShow>> year in Model.Older)
    {
        <h3><strong>@year.Key</strong></h3>
        int idx = 0;
        <div class="row">
            @foreach (var tvShow in year.Value)
            {

                <div class="col-md-2" style="text-align:center;">
                    <a href="/TVShows/Details/@tvShow.Id">
                        <img src="~/Content/Images/tvShows/@tvShow.DisplayImage" width="150" title="@tvShow.Title" alt="@tvShow.Title" /><br />
                        @tvShow.Title
                    </a>
                </div>
                idx += 1;
                if (idx % 6 == 0)
                {
                    @:</div><div class="row">
                }
            }
        </div>
    }
}
<div class="row">
    <div class="col-md-12 pull-right" style="text-align:right;">
        @foreach (string page in ViewBag.Pages)
        {
            @Html.Raw(page);
        }
    </div>
</div>
